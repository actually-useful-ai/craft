---
name: activate
description: "Start a craft session: git state, recon, CLAUDE.md audit, briefing. Alias for /craft:distill --begin. Usage: /craft:activate (also tab-completes from /craft:a)"
arguments:
  - name: input
    description: "Optional path to focus on. Defaults to cwd."
    required: false
---

Invoke the `distill` skill with `--begin` mode: $ARGUMENTS
