---
name: context
description: "Deep project memory refresh: walk every CLAUDE.md, audit nav, flag staleness. Usage: /craft:context [--deep] [path]"
arguments:
  - name: input
    description: "Optional --deep flag for full enumeration; optional path (defaults to cwd)."
    required: false
---

Invoke the `context` skill: $ARGUMENTS
